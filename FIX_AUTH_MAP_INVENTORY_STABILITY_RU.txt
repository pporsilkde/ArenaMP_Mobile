ArenaMP Mobile — auth/local-map/inventory stability fix

1. Авторизация:
   - повторное нажатие Enter/кнопки входа больше не отправляет второй password reply;
   - перед password reply больше не отправляется дублирующий PlayerBaseInfo;
   - обычная gameplay/cell синхронизация не запускается до LocalPlayer::isLoggedIn();
   - удалена принудительная отправка dynamic stats до авторизации;
   - DedicatedPlayer защищён от временного пустого/неактивного world Ptr при auth/cell переходе.

2. Локальная карта в окне карты/инвентаря:
   - пустая асинхронная OSG texture больше не кешируется как окончательная;
   - локальная карта и fog texture повторно запрашиваются, пока renderer не выдаст реальную текстуру.

3. Инвентарь:
   - paper doll (кукла) включена по умолчанию;
   - вид предметов по умолчанию Grid/значки;
   - выбранный Grid/List сохраняется в [GUI] inventory grid view.

Патч применяется поверх актуального AMP/main после патчей 06..09 и идемпотентен.
