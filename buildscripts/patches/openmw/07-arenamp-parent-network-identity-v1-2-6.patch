diff --git a/apps/openmw/mwmp/Main.cpp b/apps/openmw/mwmp/Main.cpp
index d507f96..878ba28 100644
--- a/apps/openmw/mwmp/Main.cpp
+++ b/apps/openmw/mwmp/Main.cpp
@@ -54,6 +54,9 @@ std::string Main::address = "";
 std::string Main::serverPassword = TES3MP_DEFAULT_PASSW;
 std::string Main::resourceDir = "";
 bool Main::vanillaBuildServer = false;
+std::string Main::networkVersion = "";
+int Main::networkProtocol = 0;
+std::string Main::networkCommitHash = "";
 bool Main::hideChatHistory = false;
 
 std::string Main::getResDir()
@@ -66,6 +69,21 @@ bool Main::useVanillaBuildServer()
     return vanillaBuildServer;
 }
 
+std::string Main::getNetworkVersion()
+{
+    return networkVersion.empty() ? TES3MP_VERSION : networkVersion;
+}
+
+int Main::getNetworkProtocol()
+{
+    return networkProtocol > 0 ? networkProtocol : TES3MP_PROTO_VERSION;
+}
+
+std::string Main::getNetworkCommitHash()
+{
+    return networkCommitHash;
+}
+
 bool Main::isChatHistoryHidden()
 {
     return hideChatHistory;
@@ -128,6 +146,12 @@ void Main::optionsDesc(boost::program_options::options_description *desc)
                         "сonnect to a secured server. (e.g. --password=AnyPassword")
             ("vanilla-build-server", bpo::value<bool>()->implicit_value(true)->default_value(false),
                         "use the official TES3MP 0.8.1 commit identity for older servers")
+            ("network-version", bpo::value<std::string>()->default_value(""),
+                        "override the ArenaMP/TES3MP version advertised during the RakNet handshake")
+            ("network-protocol", bpo::value<int>()->default_value(0),
+                        "override the ArenaMP/TES3MP protocol advertised during the RakNet handshake")
+            ("network-commit-hash", bpo::value<std::string>()->default_value(""),
+                        "override the ArenaMP parent commit advertised during the RakNet handshake")
             ("hide-chat-history", bpo::value<bool>()->implicit_value(true)->default_value(false),
                         "hide received chat messages while keeping chat input available");
 }
@@ -137,6 +161,9 @@ void Main::configure(const boost::program_options::variables_map &variables)
     Main::address = variables["connect"].as<std::string>();
     Main::serverPassword = variables["password"].as<std::string>();
     Main::vanillaBuildServer = variables["vanilla-build-server"].as<bool>();
+    Main::networkVersion = variables["network-version"].as<std::string>();
+    Main::networkProtocol = variables["network-protocol"].as<int>();
+    Main::networkCommitHash = variables["network-commit-hash"].as<std::string>();
     Main::hideChatHistory = variables["hide-chat-history"].as<bool>();
     resourceDir = variables["resources"].as<Files::EscapePath>().mPath.string();
 }
diff --git a/apps/openmw/mwmp/Main.hpp b/apps/openmw/mwmp/Main.hpp
index c0aba44..ef38706 100644
--- a/apps/openmw/mwmp/Main.hpp
+++ b/apps/openmw/mwmp/Main.hpp
@@ -33,6 +33,9 @@ namespace mwmp
 
         static std::string getResDir();
         static bool useVanillaBuildServer();
+        static std::string getNetworkVersion();
+        static int getNetworkProtocol();
+        static std::string getNetworkCommitHash();
         static bool isChatHistoryHidden();
 
         Networking *getNetworking() const;
@@ -48,6 +51,9 @@ namespace mwmp
         static std::string address;
         static std::string serverPassword;
         static bool vanillaBuildServer;
+        static std::string networkVersion;
+        static int networkProtocol;
+        static std::string networkCommitHash;
         static bool hideChatHistory;
         Main (const Main&);
         ///< not implemented
diff --git a/apps/openmw/mwmp/Networking.cpp b/apps/openmw/mwmp/Networking.cpp
index ece1651..1a89508 100644
--- a/apps/openmw/mwmp/Networking.cpp
+++ b/apps/openmw/mwmp/Networking.cpp
@@ -276,11 +276,12 @@ void Networking::connect(const std::string &ip, unsigned short port, std::vector
     std::string errmsg = "";
 
     std::stringstream sstr;
-    sstr << TES3MP_VERSION;
+    const std::string advertisedVersion = Main::getNetworkVersion();
+    sstr << advertisedVersion;
 
     const int advertisedProtocol = Main::useVanillaBuildServer()
         ? TES3MP_VANILLA_PROTO_VERSION
-        : TES3MP_PROTO_VERSION;
+        : Main::getNetworkProtocol();
     sstr << advertisedProtocol;
 
     std::string commitHashString;
@@ -291,6 +292,13 @@ void Networking::connect(const std::string &ip, unsigned short port, std::vector
             "Vanilla server compatibility enabled: advertising protocol %i and TES3MP commit %.10s",
             advertisedProtocol, TES3MP_VANILLA_COMMIT_HASH);
     }
+    else if (!Main::getNetworkCommitHash().empty())
+    {
+        commitHashString = Main::getNetworkCommitHash();
+        LOG_MESSAGE_SIMPLE(TimedLog::LOG_INFO,
+            "Parent ArenaMP compatibility: advertising version %s, protocol %i, commit %.10s",
+            advertisedVersion.c_str(), advertisedProtocol, commitHashString.c_str());
+    }
     else
         commitHashString = Version::getOpenmwVersion(Main::getResDir()).mCommitHash;
 
@@ -318,8 +326,8 @@ void Networking::connect(const std::string &ip, unsigned short port, std::vector
                 }
                 case ID_INVALID_PASSWORD:
                 {
-                    errmsg = "Version mismatch!\nYour client is on version " TES3MP_VERSION "\n"
-                        "Please make sure the server is on the same version.";
+                    errmsg = "Version mismatch!\nYour client advertises ArenaMP/TES3MP version " + advertisedVersion + "\n"
+                        "Please make sure the network compatibility identity matches the server.";
                     queue = false;
                     break;
                 }
