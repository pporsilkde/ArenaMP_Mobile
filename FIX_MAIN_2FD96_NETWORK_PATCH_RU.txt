ArenaMP Mobile — FIX current AMP/main network compatibility patch

Причина падения logs_86826140251:
- AMP/main корректно разрешился в 2fd96ae3570a72429085cbe85feed3212b14b202.
- Сборка падала до компиляции на git apply старого 07-arenamp-parent-network-identity-v1-2-6.patch.
- В свежем main Networking.cpp уже содержит встроенный TES3MP_COMPAT_COMMIT_HASH и изменённый handshake-код, поэтому старый hunk больше не применим.

Исправление:
- Хрупкий git patch 07 заменён на идемпотентный Python-патчер по смысловым якорям.
- Патчер сохраняет свежий AMP/main и добавляет только Android runtime overrides --network-version/--network-protocol/--network-commit-hash.
- ARENAMP_NETWORK_COMMIT теперь также записывается прямо в components/openmw-mp/Version.hpp перед компиляцией. Поэтому один выбранный compatibility SHA используют и клиент, и встроенный dedicated server.
- По умолчанию compatibility SHA: 0f659371bcbaf9e7e6b94bd6bcb7a81970082234.
- Cache epoch повышен до v1-6, чтобы CI не использовал старое уже пропатченное состояние ExternalProject.
